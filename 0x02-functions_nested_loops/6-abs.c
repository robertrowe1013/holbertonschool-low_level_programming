#include "holberton.h"
#include <inttypes.h>
/**
  * _abs - absolute value
  * @n: number
  *
  * Return: absolute value
  */
int _abs(int n)
{
	if (n < 0)
	{
		n = (n * -1);
	}
	else
	{
		;
	}

	return (n);
}
