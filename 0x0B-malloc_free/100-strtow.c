#include "holberton.h"
#include <stdlib.h>
/**
  * strtow - string tow?
  * @str: string
  *
  * Return: null if str is null, str is empty, or malloc fails, else string
  */
char **strtow(char *str)
{
	str = (void);

	return (NULL);
}
